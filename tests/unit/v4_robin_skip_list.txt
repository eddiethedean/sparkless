# Phase 6: Tests skipped when SPARKLESS_TEST_BACKEND=robin (v4 scope)
# Each line is a pattern matched against pytest nodeid (substring match).
# Empty = no tests skipped by this list. Conftest no longer applies this file.
# See docs/v4_behavior_changes_and_known_differences.md
